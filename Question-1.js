// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

let name = "yasir";
let signedin = true;
let loginNum;
let obj = null;

console.log(`
${name} = ${typeof name}
${signedin} = ${typeof signedin}
${loginNum} = ${typeof loginNum}
${obj} = ${typeof obj}
`);
